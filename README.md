# MedSocket

## How to

- Installing dependencies

  With yarn:

  ```bash
  yarn
  ```

  Or with npm:

  ```bash
  npm install
  ```

- Running the code

  With yarn:

  ```bash
  yarn start
  ```

  Or with npm:

  ```bash
  npm start
  ```
